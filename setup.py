from setuptools import setup

setup(name='ctool',
      version=1.0,
      description='Tool to estimate the continuum of a line spectrum observed with JWST',
      author='Klaus Pontoppidan (JPL)',
      author_email='klaus.m.pontoppidan@jpl.nasa.gov',
      download_url = '',
      packages=['ctool'],
      install_requires=[]
      )
